// Put your OpenAI API key here before running the app locally.
//
// Important: a key compiled into a mobile app can be extracted from the APK/IPA.
// This is fine for personal testing, but use a small backend proxy before
// sharing the app publicly.
const String openAiApiKey = '';

// Vision-capable model used with the OpenAI Responses API.
const String openAiModel = 'gpt-4.1-mini';
